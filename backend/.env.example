
PORT=5000

MONGODB_URI=your_mongodb
MONGODB_DB_NAME=your_db_name

# Product images uploaded in admin are stored as base64 in MongoDB (max ~2.5MB per image).
# Ensure express JSON limit (10mb in src/index.js) is sufficient for your uploads.

RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_secret_key
